# Native Runtime Notes

The native runtime is scaffolded for a Linux-first demonstration path:
- C stencil kernel
- CMake presets
- future pthread scheduling
- future shared-memory telemetry and socket monitor

Phase 0 keeps the native path intentionally small and buildable.
